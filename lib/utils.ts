import { type ClassValue, clsx } from "clsx"
import { twMerge } from "tailwind-merge"

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// ### Solution:

// Create a new file at `lib/utils.ts` with the following content:
// ```typescript
// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }
// ```

// Fixing the Blank Page Issue in Your Next.js Project

// After examining your project files, I've identified several issues that are likely causing your blank page problem. Let's fix them:

// ## 1. Issue with `lib/utils.ts` File

// I noticed that your `lib/utils.ts` file contains commented code that should be active. This is critical because the `cn` utility is used in all your UI components.
// ```typescript
// import {
